def print_string(param_str, column_count, row_count):
    """この関数は5文字以上10文字以下でなければエラーメッセージを表示させ、アルファベット(a～z, A～Z)以外が使われていればエラーメッセージを表示させる。


    Args:
        param_str (_type_): 文字列
        column_count (_type_): 列を繰り返す
        row_count (_type_): 行を繰り返す
    """
    #TODO:未実装、実装予定